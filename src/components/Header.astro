---
import { navigation, siteData } from "../data/site";

const pathname = Astro.url.pathname;
const isHome = pathname === "/";
---

<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href="/" aria-label={siteData.name}>
      <img src="/logo.svg" alt="" class="brand-logo" width="48" height="48" />
    </a>

    <nav aria-label="Primary navigation">
      <ul class="nav-list">
        {
          navigation.map((item) => {
            const isActive =
              item.href === "/"
                ? pathname === item.href
                : pathname.startsWith(item.href);

            // On home page use anchor links; on sub-pages use full page links
            const href = isHome ? (item.anchor ?? item.href) : item.href;

            return (
              <li>
                <a
                  href={href}
                  class:list={{ active: isActive }}
                  aria-current={isActive ? "page" : undefined}
                >
                  {item.label}
                </a>
              </li>
            );
          })
        }
      </ul>
    </nav>
  </div>
</header>
